-- me_bridge_chemicals_monitor.lua
local mon = peripheral.find("monitor")
local bridge = peripheral.find("me_bridge")

if not mon then
    print("Kein Monitor gefunden!")
    return
end
if not bridge then
    mon.clear()
    mon.setCursorPos(1,1)
    mon.write("Kein me_bridge gefunden!")
    return
end

mon.clear()
mon.setBackgroundColor(colors.black)
mon.setTextColor(colors.white)
mon.setTextScale(1)
local w,h = mon.getSize()
local cursorY = 1

local function monPrint(text)
    if cursorY > h then
        mon.scroll(1)
        cursorY = h
        mon.setCursorPos(1,cursorY)
        mon.clearLine()
    end
    mon.setCursorPos(1,cursorY)
    mon.write(text)
    cursorY = cursorY + 1
end

local function formatBytes(bytes)
    if bytes >= 1000000 then
        return string.format("%.1fM", bytes/1000000)
    elseif bytes >= 1000 then
        return string.format("%.1fK", bytes/1000)
    else
        return tostring(bytes)
    end
end

-- Berechne Chemical Storage
local usedChem = 0
local freeChem = 0

-- Gesamtnutzung ermitteln
local okUsed, used = pcall(bridge.getUsedChemicalStorage, bridge)
local okTotal, total = pcall(bridge.getTotalChemicalStorage, bridge)
usedChem = okUsed and used or 0
freeChem = okTotal and total or 0

-- Zellen prüfen
if type(bridge.getCells) == "function" then
    local ok, cells = pcall(bridge.getCells, bridge)
    if ok and type(cells) == "table" then
        monPrint("=== CHEMICAL CELLS ===")
        for i, cell in ipairs(cells) do
            local ctype = cell.cellType or cell.type or "unknown"
            if ctype == "chemical" then
                local totalBytes = cell.totalBytes or cell.bytes or 0
                local partitioned = cell.isPartitioned or cell.partitioned or false
                local precise = cell.isPrecise or cell.precise or false
                local includedPrecise = cell.isIncludedPrecise or cell.includePrecise or false

                -- Free Storage nur abziehen, wenn Partitioned+Precise UND nicht IncludedPrecise
                if partitioned and precise and not includedPrecise then
                    freeChem = freeChem - totalBytes
                    if freeChem < 0 then freeChem = 0 end
                end

                local item = cell.item or cell.name or "nil"
                monPrint(string.format(
                    "Cell #%d: name=%s | totalBytes=%s | Partitioned=%s | Precise=%s | IncludedPrecise=%s",
                    i, item, formatBytes(totalBytes), tostring(partitioned), tostring(precise), tostring(includedPrecise)
                ))
            end
        end
    else
        monPrint("getCells() Fehler: " .. tostring(cells))
    end
end

-- Storage anzeigen
monPrint("")
monPrint(string.format("Used Chemical Storage: %s", formatBytes(usedChem)))
monPrint(string.format("Free Chemical Storage: %s", formatBytes(freeChem)))
