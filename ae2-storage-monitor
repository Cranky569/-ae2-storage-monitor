local mon = peripheral.find("monitor")
local bridge = peripheral.find("me_bridge")

if not mon then return end
if not bridge then
    mon.clear()
    mon.setCursorPos(1,1)
    mon.write("Kein me_bridge gefunden!")
    return
end

mon.clear()
mon.setBackgroundColor(colors.black)
mon.setTextColor(colors.white)
mon.setTextScale(1)
local _, h = mon.getSize()
local cursorY = 1

local function monPrint(text)
    if cursorY > h then
        mon.scroll(1)
        cursorY = h
    end
    mon.setCursorPos(1, cursorY)
    mon.clearLine()
    mon.write(text)
    cursorY = cursorY + 1
end

local ok, cells = pcall(bridge.getCells, bridge)
if not ok then
    monPrint("Fehler beim Abrufen von Cells: " .. tostring(cells))
    return
end

monPrint("=== Chemical Cells ===")
for i, cell in ipairs(cells) do
    local ctype = cell.cellType or cell.type
    if ctype == "chemical" or ctype == "c" then
        monPrint("Chemical Cell #" .. i)
        for k,v in pairs(cell) do
            monPrint("  " .. tostring(k) .. " = " .. tostring(v))
        end
        monPrint("----------------")
    end
end
