-- chemical_cells_debug.lua
-- Zeigt, welche Felder getCells() für Chemical Cells liefert (Partitioned / IncludedPrecise / Excluded)

local mon = peripheral.find("monitor")
local bridge = peripheral.find("me_bridge")

if not mon then
    print("Kein Monitor gefunden!")
    return
end

if not bridge then
    print("Kein me_bridge gefunden!")
    return
end

mon.clear()
mon.setBackgroundColor(colors.black)
mon.setTextColor(colors.white)
mon.setTextScale(1)
local w,h = mon.getSize()
local cursorY = 1

local function monPrint(text)
    if cursorY > h then
        mon.scroll(1)
        cursorY = h
        mon.setCursorPos(1,cursorY)
        mon.clearLine()
    end
    mon.setCursorPos(1,cursorY)
    mon.write(text)
    cursorY = cursorY + 1
end

-- getCells prüfen
if type(bridge.getCells) ~= "function" then
    monPrint("getCells() ist auf dieser Bridge nicht verfügbar")
    return
end

local ok, cells = pcall(bridge.getCells, bridge)
if not ok then
    monPrint("Fehler beim Abrufen der Cells: " .. tostring(cells))
    return
end

if type(cells) ~= "table" then
    monPrint("getCells() hat keine Tabelle zurückgegeben")
    return
end

monPrint("=== Chemical Cells Partition Debug ===")
for i, cell in ipairs(cells) do
    local ctype = cell.cellType or cell.type or "unknown"
    if ctype == "chemical" or ctype == "c" then
        local name = cell.item or cell.name or "nil"
        local partitioned = cell.isPartitioned or cell.partitioned or false
        local includedPrecise = cell.isIncludedPrecise or cell.includePrecise or false
        local excluded = cell.isExcluded or cell.excluded or false

        monPrint(string.format(
            "Cell #%d: name=%s | Partitioned=%s | IncludedPrecise=%s | Excluded=%s",
            i, name, tostring(partitioned), tostring(includedPrecise), tostring(excluded)
        ))
    end
end
