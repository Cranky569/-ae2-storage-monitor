-- me_bridge_chemical_debug.lua
-- Zeigt alle Infos zu Chemical Storage Cells auf dem Monitor an, ohne Berechnung

-- Monitor vorbereiten
local mon = peripheral.find("monitor")
local bridge = peripheral.find("me_bridge")

if not mon then
    print("Kein Monitor gefunden!")
    return
end

if not bridge then
    mon.clear()
    mon.setCursorPos(1,1)
    mon.write("Kein me_bridge gefunden!")
    return
end

mon.clear()
mon.setBackgroundColor(colors.black)
mon.setTextColor(colors.white)
mon.setTextScale(1)
local w, h = mon.getSize()
local cursorY = 1

local function monPrint(text)
    if cursorY > h then
        mon.scroll(1)
        cursorY = h
        mon.setCursorPos(1, cursorY)
        mon.clearLine()
    end
    mon.setCursorPos(1, cursorY)
    mon.write(text)
    cursorY = cursorY + 1
end

-- Alle Chemical Cells auflisten
if type(bridge.getCells) == "function" then
    local ok, cells = pcall(bridge.getCells, bridge)
    if ok and type(cells) == "table" then
        monPrint("=== Chemical Cells Debug ===")
        for i, cell in ipairs(cells) do
            -- Prüfen ob es eine Chemical Cell ist
            local ctype = cell.cellType or cell.type or "unknown"
            if ctype == "chemical" then
                local name = cell.item or cell.name or "nil"
                local partitioned = cell.isPartitioned or cell.partitioned or false
                local includedPrecise = cell.isIncludedPrecise or cell.includePrecise or false
                local excluded = cell.isExcluded or cell.excluded or false
                local totalBytes = cell.totalBytes or cell.bytes or 0

                monPrint(string.format(
                    "Cell #%d: name=%s | totalBytes=%s | Partitioned=%s | IncludedPrecise=%s | Excluded=%s",
                    i, name, totalBytes, tostring(partitioned), tostring(includedPrecise), tostring(excluded)
                ))
            end
        end
    else
        monPrint("Fehler beim Abrufen von Cells: " .. tostring(cells))
    end
else
    monPrint("getCells() nicht verfügbar auf dieser Bridge")
end
