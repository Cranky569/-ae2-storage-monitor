-- Nur relevante Methoden für Chemical Storage anzeigen
monPrint("=== ME Bridge Methoden für Chemical Storage ===")
local methods = peripheral.getMethods(peripheral.getName(bridge))
for _, m in ipairs(methods) do
    if m == "getUsedChemicalStorage" or m == "getTotalChemicalStorage" then
        monPrint("- " .. m)
    end
end
